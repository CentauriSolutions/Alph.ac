%h1
  Short link for #{@link.url}
  - if user_signed_in? && @link.user == current_user
    %small.text-muted= link_to "Edit",  edit_link_path(@link)
%h2 Clicks:
%dl.row
  .col-sm-4
    %table.table.table-striped
      %thead
        %tr
          %td Date
          %td Count
      - @link.clicks.group_by_day(:created_at).count.each do |date, count|
        %tr
          %td= date.strftime("%B %-d, %Y")
          %td= count
